export const LOGGED_IN_REDIRECT_URL = "/";

/**
 * these routes are accessible only to admins
 */
export const ADMIN_ROUTES = ["/teach"];

/**
 * These routes are accessible to everyone
 */
export const PUBLIC_ROUES = ["/api/webook", "/", "/courses/*"];
